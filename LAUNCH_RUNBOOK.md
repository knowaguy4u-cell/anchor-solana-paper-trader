# Anchor — Launch Runbook (Tim's taps)

Everything below is yours. Nothing here was submitted, posted, or published —
the entry is built and waiting.

## 0. What you're submitting

**Anchor**: an autonomous Solana paper-trading agent with a strict risk
policy and a public trade journal — Steve's disciplined sibling, built for
the Steve Agent Arena. Working code, a runnable 18-cycle demo, and full docs
live in `~/workspace/your_files/money-pipeline/bounty-scout/steve-arena/`.

## 1. Review it (5 min)

- Read `README.md` in the folder above.
- Run the demo yourself if you want: `cd agent && node demo/run-demo.ts`
  (needs Node 22.6+; any laptop works).

## 2. Put it on GitHub (your tap)

The code is local-only right now. Push it to your GitHub so the submission
has a repo link:

1. Create a repo at github.com/knowaguy4u-cell (e.g. `anchor-agent`).
2. Push the contents of the `steve-arena/` folder.
3. Optional but strong: record a 2-minute screen capture of
   `node demo/run-demo.ts` running and talking through it — judges watch
   demos. Upload unlisted to YouTube or attach the file.

## 3. Sign in to Superteam Earn (your tap)

1. Go to https://superteam.fun/earn/listing/steve-agent-arena-launch-your-agent-and-win-500-usdc
2. Sign in (wallet or email — your call).
3. **Deadline: Sep 20, 2026 at 4:59 PM CDT** (21:59 UTC). Submit early —
   don't leave it to the last hour.

## 4. Submit (your tap)

On the listing page, submit with:
- **Title:** from `SUBMISSION.md`
- **Description:** paste from `SUBMISSION.md`
- **Links:** your GitHub repo URL + demo video URL

Submission text is ready to copy-paste in `SUBMISSION.md`.

## 5. Get paid (if you win)

Winners announced by **Sep 27, 2026**. Prizes: 250 / 150 / 100 USDC for
1st / 2nd / 3rd. Connect a Solana wallet on your Earn profile to receive
USDC — that's a signing tap in your wallet, nothing leaves your control.

## If the listing looks different than expected

The listing page is JS-heavy and I couldn't read its full description text —
only the metadata (prizes, deadline, AGENT_ALLOWED flag, sponsor). Before
submitting, skim the listing body once: if it asks for something specific
I didn't build (e.g. "deploy to X" or "post on Y"), tell me and I'll close
the gap before the deadline. The code is modular enough to extend fast.
